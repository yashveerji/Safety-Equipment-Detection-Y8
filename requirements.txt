opencv-python>=4.9.0.80,<5.0.0  # OpenCV core
cvzone==1.5.1                  # Utility overlay helpers
ultralytics>=8.2.0,<9.0.0      # YOLOv8 framework
numpy>=1.24.0,<2.0.0           # Numerical ops

# PyTorch (choose CUDA build manually if GPU available). Pin to compatible versions.
torch>=2.1.0,<2.3.0
torchvision>=0.16.0,<0.19.0

# If installation issues on Windows with GPU, consult: https://pytorch.org/get-started/locally/

# Web / API dependencies
fastapi>=0.110.0,<0.115.0
uvicorn[standard]>=0.23.0,<0.32.0
python-multipart>=0.0.9,<0.0.10
pydantic>=2.0.0,<3.0.0
